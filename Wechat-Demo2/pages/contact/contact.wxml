<!--pages/contact/contact.wxml-->

<block>
    <view>{{count}}</view>
    <button type="primary" bind:tap="add" size="mini"> + </button>
</block>

<view class="box" wx:for="{{ colorList }}" style="background-color: {{ item }};" wx:key="idex"> 
  {{ item }}
</view>